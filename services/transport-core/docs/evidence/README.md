# Evidence Artifacts

Store reproducible benchmark captures generated by `scripts/benchmark.sh`.

Each run should include:
- relay log
- sender/receiver logs for `real-time`
- sender/receiver logs for `buffered`
- output binaries for sanity checks
